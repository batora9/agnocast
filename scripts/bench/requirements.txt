# Ubuntu 22.04's python3-matplotlib (3.5.x) is compiled against the numpy 1.x
# ABI and fails at import under numpy 2. Pin below 2 unless matplotlib is also
# installed from PyPI.
numpy>=1.21,<2
pandas>=1.3
matplotlib>=3.5
